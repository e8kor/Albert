import common._

name := "rtp-plugin"

(common velocityLibraries)

(common poiLibraries)

(common macrosSettings)

(common utilityLibraries)

(common akkaLibraries)

(common configLibraries)

(common buildInfoPackagePath) := "org.system.plugin.rtp.info"

(common buildInfoPluginSettings)